// Hier moet ik de functie plaatsen om op te tellen
const optellen = (getal1, getal2) => {
  return getal1 + getal2;
};

const aftrekken = (getal1, getal2) => {
  return getal1 - getal2;
};

// hier moet ik zeggen welke functies de externe partner mag/kan gebruiken
module.exports = { optellen, aftrekken };
